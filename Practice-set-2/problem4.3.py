# 3 - Use a while loop to reverse a given number (e.g., 123 → 321).

num = 79899

print (int(str(num)[::-1]))

# First we are converting the integer into a string and with string you can do advanced slicing, then later you can convert it back to an integer.